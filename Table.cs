using UnityEngine;

public class Table : MonoBehaviour
{
    public bool IsAvailable { get; set; } = true;

    private void Start()
    {
        // Можно инициализировать доступность стола здесь, если необходимо
    }
}